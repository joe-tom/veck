

function Vecky(vector) {
  var vectorArray = []

  switch (typeof vector) {
    case 'number':

    break;
    case 'string':

    break;
    case 'object':
      if (vector instanceof Array) {

      }
    break;
  }
}